import routes from './routes'
import scrollBehavior from './scrollBehavior'

const RouterConfig = {
  // mode: 'history',
  base: '/jsl-webpack-vue-h5/',
  routes,
  scrollBehavior
}

export default RouterConfig
